# ─────────────────────────────────────────────
# ReUse360 Plus — Environment Variables
# Copy to .env.local and fill in real values
#
# HOW TO GET CLERK KEYS:
#   1. Go to https://clerk.com
#   2. Create account → Create application → name it "reuse360-plus"
#   3. Go to API Keys tab
#   4. Copy NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY (starts with pk_test_)
#   5. Copy CLERK_SECRET_KEY (starts with sk_test_)
#   6. Go to Webhooks → Add Endpoint → URL: https://yourdomain.com/api/auth/webhook
#      Events: user.created, user.updated, user.deleted → copy signing secret
# ─────────────────────────────────────────────

# ── DATABASE ──────────────────────────────────
# Local dev (Docker Compose)
DATABASE_URL="postgresql://reuse360:reuse360dev@localhost:5432/reuse360"
DIRECT_URL="postgresql://reuse360:reuse360dev@localhost:5432/reuse360"

# Production (Render Postgres) — replace with your Render connection string
# DATABASE_URL="postgresql://user:pass@host:5432/dbname?sslmode=require"
# DIRECT_URL="postgresql://user:pass@host:5432/dbname?sslmode=require"

# ── CLERK AUTH ────────────────────────────────
# ⚠ REPLACE THESE — get from clerk.com → your app → API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_REPLACE_WITH_YOUR_CLERK_PUBLISHABLE_KEY
CLERK_SECRET_KEY=sk_test_REPLACE_WITH_YOUR_CLERK_SECRET_KEY
CLERK_WEBHOOK_SECRET=whsec_REPLACE_WITH_YOUR_WEBHOOK_SIGNING_SECRET

# Clerk redirect URLs (do not change for local dev)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/onboarding

# ── APP ───────────────────────────────────────
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ── BEACON AMI ───────────────────────────────
# Get from beaconama.net → Users → API Clients
BEACON_API_URL=https://api.beaconama.net
BEACON_CLIENT_ID=REPLACE_WITH_BEACON_CLIENT_ID
BEACON_CLIENT_SECRET=REPLACE_WITH_BEACON_CLIENT_SECRET
BEACON_TENANT_ID=REPLACE_WITH_BEACON_TENANT_ID
BEACON_SERVICE_POINT_CYCLE=REPLACE_WITH_CYCLE_ID

# ── CITYWORKS ─────────────────────────────────
# Get from Cityworks admin or IT — Pinellas County AMS
CITYWORKS_BASE_URL=https://cityworks.pinellascounty.org
CITYWORKS_USERNAME=REPLACE_WITH_CITYWORKS_USERNAME
CITYWORKS_PASSWORD=REPLACE_WITH_CITYWORKS_PASSWORD
CITYWORKS_SR_TEMPLATE_ID=REPLACE_WITH_SR_TEMPLATE_ID
CITYWORKS_TEST_MODE=true

# ── MAP TILES ─────────────────────────────────
# Optional — get free key at maptiler.com (100k tiles/month free)
# Falls back to OpenStreetMap if not set
NEXT_PUBLIC_MAPTILER_API_KEY=REPLACE_WITH_MAPTILER_KEY

# ── DOCKER QUICK REFERENCE ───────────────────
# Start:  docker compose up -d
# Stop:   docker compose down
# Reset:  docker compose down -v
# pgAdmin: http://localhost:5050 (admin@reuse360.local / reuse360admin)
